# Infrastructure section README.md

- ./environments

This folder contains the definitions of the resources which are included by `source` and defined as `module`.
The `source` points to the ./modules folder.